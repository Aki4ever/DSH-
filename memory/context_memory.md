# 全局上下文记忆与长期偏好沉淀 (Context & Preference Memory)

本文档是智能体跨会话的**长期上下文记忆库 (Long-term Memory)**，记录全局环境的核心约定、用户个性化偏好以及固化的系统运行基线。每次开启新会话时，开箱自检协议将主动读取本文档，实现“记忆不丢失、偏好不重设”。

---

## 📌 一、全局核心约定与硬性偏好

| 约定领域 | 核心规则 | 执行细节与约束 |
| :--- | :--- | :--- |
| **交互语言** | **全中文通俗表达** | 所有输出、问答、汇报与工程文档必须使用规范中文，语言通俗直白、结构简单，坚决杜绝生僻字和黑话。 |
| **任务命名** | **三要素强制齐备** | 格式为 `[任务名][任务难度] 任务概述`，首轮定标后必须立即调用 [`scripts/rename_session.sh`](../scripts/rename_session.sh) 锁定。格式细则唯一权威源：[`knowledge/common/task_naming_spec.md`](../knowledge/common/task_naming_spec.md)。 |
| **难度打分** | **100分制量化打分** | 四维模型（范围0~30 + 依赖0~30 + 风险0~20 + 复杂度0~20）。≤35分走快速轻量流，>35分走标准完备六步流。 |
| **图形组件** | **原生图形化区块卡片** | 启动（▶ 🚀）、阶段（● 进度条）、完成（■ 🏁）；严禁输出裸 HTML 标签（如 details/div），统一使用 Markdown 引用色条卡片。 |
| **安全与权限** | **免审批完全自律** | `Approval: never` + `danger-full-access` 环境下，禁止暴力清理根目录与宿主外部目录；敏感文件先读后改。 |
| **台账同步** | **需求-规则双向同步** | 任何规则与重大机制调整，必须先登记 `docs/requirements.md`，执行完毕后回写并提交 Git。 |

---

## 🧭 二、短期记忆与长期记忆协同运作机制

```text
[当前会话交互 (短时记忆)]
  - Sticky 吸顶置顶组件（视口常驻当前工单、执行阶段与核心交付）
  - Todo 任务清单（跟踪单一在办事项与即时流转）
         │
         │ 任务闭环归卷 (归) 提炼沉淀
         ▼
[跨会话持久化 (长期记忆)]
  - memory/context_memory.md (长期约定与偏好)
  - memory/lessons_learned.md (避坑经验与系统特性)
  - docs/requirements.md (需求台账与历史溯源)
```

---

## 🏷️ 三、用户工作区与环境常驻配置

- **工作区路径**：`/Users/linqiyu/Documents/DSH/全局规则`
- **默认 Git 分支**：`main` (与远程 `origin/main` 保持对齐)
- **DSH Web URL**：`http://127.0.0.1:50447`
- **主要模型**：`gemini-3.8-flash-high`（已开启文本与图片多模态）
