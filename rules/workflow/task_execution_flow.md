# 任务执行结构化流程与双轨分流法典 (Task Execution Flow)

> ### 🏷️ **版本信息与实施追踪**
> - **当前文档版本**：`v2.9.0`
> - **对应实施版本**：`v2.9.0`
> - **版本治理规范**：遵循 [`rules/workflow/versioning_standard.md`](versioning_standard.md)
> - **生效状态**：`[Release 稳定生效]`

本文档是系统全域任务执行的**最高实操法典**，确立**强制硬约束流程 (Hard Line)** 与 **敏捷捷径通道 (Fast Track)** 的显式分层判定，并规范**全生命周期 DSH 原生可视化组件与插槽的 100% 全装配标准**。

---

## 🚦 一、执行双轨分层与判定准则

```text
                       ┌── [难度 ≤ 35分 · 满足捷径条件] ──▶ ⚡ 敏捷捷径通道 (Fast Track)
[输入新任务] ── 意图与打分 ──┤
                       └── [难度 > 35分 · 触碰任一红线] ──▶ 🚨 强制硬约束流 (Hard Line)
```

### 1. 强制必走流程 vs 捷径通道对照矩阵

| 执行环节 | 🚨 强制硬约束流程 (Hard Line) | ⚡ 敏捷捷径通道 (Fast Track) |
| :--- | :--- | :--- |
| **触发门槛** | 难度 $> 35$ 分；或触及跨模块重构、核心规则修改、代码新增、发布升版 | 难度 $\le 35$ 分；纯信息检索、单文件局部微调、文字修正、无害巡检 |
| **1. 会话重命名** | **必须首发调用** `./scripts/rename_session.sh` 锁定 8 字以内标题 | **豁免**：直接沿用当前会话标题，无需首发改名 |
| **2. 知识库前置审查** | 强制检索 `knowledge/` 与元规则防冲突，遇冲突立即阻断 | **豁免**：纯技术或格式查询直接作答，免业务审查 |
| **3. 前置风险评估** | **必须显式输出**《前置四维风险评估卡》与回滚预案 | **捷径**：免输出独立大表格，以行内 1 句简述风险与防御 |
| **4. Todo 状态跟踪** | **必须调用** `todo_write` 激活原生 `TodoPanel` 状态条 | **捷径**：单步或简单双步任务免调用 `todo_write` |
| **5. 写后读回校验** | 凡写必读，修改后必须 `read` 读回确认完整性 | 保持一致（修改文件必读回校验，禁止盲目交付） |
| **6. 自动化测试门禁** | **必须编写/运行真实测试脚本**，100% 绿灯方可结项 | **捷径**：纯文档/配置微调，通过终端命令或读回校验即算通过 |
| **7. 页面台账与截图** | 涉及 Web/GUI 变动，强制截图并登记 `Page_*.png` 资产台账 | 非 GUI 前端任务与纯规则任务，完全免除截图与台账 |
| **8. 需求台账与升版** | **三位一体原子同步**：更新 `docs/requirements.md` 并推进版本号 | **捷径**：纯答疑、无实际文件持久化修改的任务免写台账 |
| **9. 收尾复盘与看板** | 强制输出三维 AAR 复盘卡与 M1~M6 效率审计指标看板 | **捷径**：免冗长 AAR 复盘与六维看板，输出精简交付总结 |

---

## ⚙️ 二、强制硬约束流程：标准六阶十六步流水线

当任务判定为 **Hard Line** 时，依序执行以下十六道工序。**每道工序的强制等级取决于它有没有客观判定手段**：

- 🟢 **强制**：有脚本或明确命令可判定"做了没有、做对没有"，未完成不得推进；判定一律以命令输出为准，不采信口头声明；
- 🟡 **建议**：尚无独立判定手段，按建议执行（做了更好，不做不算违约）；这类工序仍必须避免"只写不做"。

**为什么这样分**：没有判定手段却写成"必须"，等于把约束变成一句口号——既检查不了，也无法追责。
凡后续为某道工序补上了判定脚本，就把该行等级升回 🟢 强制（判定手段列必须填真实存在的路径）。

| 阶段 | 工序号 | 工序名称与核心动作 | 判定手段（🟢 强制 / 🟡 建议） |
| :---: | :---: | :--- | :--- |
| **一、探境** | **S01** | 环境权限与全中文说明自检 | 🟡 建议（按 `AGENTS.md` 红线与语言规范执行） |
| | **S02** | 干道路由与业务基线检索 | 🟡 建议（**判定**：`indexes/shortcuts_index.md` 是否命中） |
| **二、定标** | **S03** | 需求意图提炼与查重 | 🟢 强制（**判定**：`node scripts/redundancy_scan.mjs --root .` 无高相似对） |
| | **S04** | 双螺旋决策打分与轨道判定 | 🟢 强制（**判定**：出分过程必须可见，轨道判定与后续动作一致） |
| | **S05** | 首动握手重命名门禁 | 🟢 强制（**判定**：首个 bash 调用为 `./scripts/rename_session.sh`，实测返回成功） |
| | **S06** | 核心工单与立项赋能确立 | 🟡 建议（新项目立项时以 `templates/project_dsh_bootstrap_template.md` 为准） |
| **三、筹策** | **S07** | 任务分解与 Todo 初始化 | 🟢 强制（**判定**：存在 `todo_write` 调用且状态可查） |
| | **S08** | 风险揭示与全局资源防冲突锁定 | 🟢 强制（**判定**：`./scripts/global_scheduler_lock.sh` 申领成功） |
| **四、攻坚** | **S09** | 上下文指纹单次读取 | 🟡 建议（同一文件重复读取不报错但浪费预算） |
| | **S10** | 精准修改与遇碰即对齐 | 🟡 建议（对齐结果由 S13 的存量校准统一判定） |
| | **S11** | 写后强制读回与指纹校验 | 🟢 强制（**判定**：每个被改文件均有对应 `read` 读回记录） |
| **五、质检** | **S12** | 自动化测试与质量门禁 | 🟢 强制（**判定**：测试脚本实跑 100% 绿灯，贴出真实输出） |
| | **S13** | 存量排查与指纹台账同步 | 🟢 强制（**判定**：`node scripts/legacy_align_scan.mjs --root .` 与 `node scripts/conflict_scan.mjs --root .` 清零；`./scripts/fingerprint_audit.sh --scan` 更新完成） |
| **六、归卷** | **S14** | 三位一体版本号强同步 | 🟢 强制（**判定**：台账条目版本与文档头部版本一致） |
| | **S15** | 远程 Git 强同步与释放排他锁 | 🟢 强制（**判定**：`./scripts/git_sync_remote.sh` 推送成功回执） |
| | **S16** | 可点击交付与 AAR 复盘 | 🟢 强制（**判定**：产出文件以行内代码引用，`./scripts/control_gates.sh badge` 结果已贴出） |

**结算**：🟢 强制 11 道 · 🟡 建议 5 道（S01 / S02 / S06 / S09 / S10）。
原表述为"十六步全部必须、禁止跳步"，与磁盘上只有 4 项门禁判定的事实不符；
现改为"有判定手段者强制、无判定手段者建议"，并保留升级通道。

---

## 🔍 二之一、流程有效执行的核心判定命令（一条对一条）

上位判定只认命令输出，不认口头声明。**这些命令在任何环境都不得跳过**（相对项目根执行）：

| 判定什么 | 命令 | 通过标准 |
| :--- | :--- | :--- |
| 门禁是否全过 | `./scripts/control_gates.sh check` | G1~G4 全部 ✅，`execAllowed` 为真 |
| 是否重复造轮子 | `node scripts/redundancy_scan.mjs --root .` | 高相似块对 = 0（有重复则先合并） |
| 事实是否自相矛盾 | `node scripts/conflict_scan.mjs --root .` | 冲突数 = 0（有冲突则先出裁决方案） |
| 存量是否跟上新规范 | `node scripts/legacy_align_scan.mjs --root .` | 待对齐数 = 0（未清零须书面说明原因） |
| 资产是否暗中漂移 | `./scripts/fingerprint_audit.sh --scan` | 台账更新完毕，Stale 项有对齐计划 |
| 进度徽标 | `./scripts/control_gates.sh badge` | 输出一行式进度，贴进收尾 |

> 说明：以上六条命令是"流程真的执行了"的唯一客观证据。
> 十六步中未列入本表的工序（S01 / S02 / S06 / S09 / S10）即为 🟡 建议等级。

---

## 🎨 三、全生命周期 DSH 原生可视化组件装配规范

为充分发挥 DSH 宿主前端的原生可视化能力，确立以下组件绑定规范。
**强制等级说明**：涉及"用户能否看见进度与证据"的组件为 🟢 强制（这类组件缺失等于过程不可核查）；
纯渲染偏好为 🟡 建议（好看但缺了不影响正确性）。

| 交互与执行阶段 | 必须调用的工具 / 语法规范 | 对应激活的 DSH 原生可视化组件 (Slot) | 强制等级 |
| :--- | :--- | :--- | :---: |
| **1. 侧边栏标题投影** | `./scripts/rename_session.sh` | `sidebar.workspaces` 侧边栏会话节点 | 🟢 强制（S05 判定） |
| **2. 长期自主目标治理** | `create_goal` / `update_goal` | `conversation.input.dock` 的 `GoalBar` (order 10) | 🟡 建议（长任务推荐） |
| **3. 多步骤实时进度条** | `todo_write` | `conversation.input.dock` 的 `TodoPanel` (order 0) | 🟢 强制（S07 判定） |
| **4. 终端系统命令执行** | `bash` / `pwsh` 原生工具 | `tool.call.toolview` 的 `TerminalCard` | 🟢 强制（判定命令须贴真实输出） |
| **5. 文件检阅与源码阅读** | `read` 原生工具 | `tool.call.toolview` 的 `ReadCard` | 🟢 强制（S11 读回校验） |
| **6. 文件补丁与修改编写** | `edit` / `write` 原生工具 | `tool.call.toolview` 的 `DiffCard` | 🟢 强制（改动须可审阅） |
| **7. 模式扫描与正则检索** | `grep` / `glob` 原生工具 | `tool.call.toolview` 的 `SearchCard` | 🟡 建议 |
| **8. 外部网络信息检索** | `web_search` 原生工具 | `tool.call.toolview` 的 `WebCard` | 🟡 建议（涉外部信息时推荐） |
| **9. 交互式澄清与表单提问**| `ask_user_question` 原生工具 | `conversation.composer` 的 `UserQuestionsView` | 🟡 建议（有歧义时推荐） |
| **10. 轮次产出物直达跳转** | 收尾回复中以 `` `path/file` `` 引用文件 | `conversation.chat.turnTail` (deliverables 插件) | 🟢 强制（S16 判定） |
| **11. 结构化卡片排版** | GFM 二级引用块 `> ### 📌 ...` 与表格 | 聊天流全局渲染高亮色条卡片 | 🟡 建议 |

---

## ⚡ 四、敏捷捷径通道：极简三步流

当任务判定为 **Fast Track** 时，执行极简三步闭环：
1. **【探】(快速感知)**：通过 `read` 或 `grep` 定位目标，激活 ReadCard，行内确认无高危风险；
2. **【攻】(精准落地)**：通过 `edit` 激活 DiffCard 进行最小粒度修改，紧随 `read` 读回确认；
3. **【归】(精炼交付)**：输出结构化交付说明与可点击链接，跳过冗长看板，快速响应。

---

## 🏷️ 五、任务打分与命名规范

### 1. 100 分制难度打分模型
$$\text{难度总分} = \text{影响范围}(0\sim30) + \text{依赖深度}(0\sim30) + \text{安全风险}(0\sim20) + \text{技术复杂度}(0\sim20)$$
- **[35~100分]**：自动进入 **Hard Line** 强制完备流；
- **[0~34分]**：自动进入 **Fast Track** 敏捷捷径通道。

### 2. 会话命名格式（≤ 8 汉字）
格式强制为：`[分类编号][难度分] 8字概述`
- 示例：`[R001][60分] 任务流程重构` (8字) ｜ `[O002][25分] 磁盘周期清理` (8字)

---

## 🛡️ 六、执行安全与防死循环铁律

1. **两步无进展熔断 (Circuit Breaker)**：若同一错误连续尝试两次无增量，必须立即阻断并报错，禁止盲目反复重试；
2. **绝对白名单保护**：执行任何清理或批量修改时，`knowledge/`、`memory/`、`rules/`、`indexes/`、`docs/` 核心资产绝对禁止删除；
3. **GFM 原生组件规范**：禁止输出裸 HTML 标签，一律使用标准 Markdown 引用块（`>`）与表格呈现视觉卡片。
