# ==============================================================================
# 存量对齐 · 书面说明豁免登记
# ==============================================================================
# 规范依据：rules/workflow/versioning_standard.md 要求待对齐项
#           「须清零**或书面说明原因**」。本文件承载后者。
#
# 为什么需要本文件：没有它只剩两种坏结果 ——
#   要么改检测器放水（灵敏度下降、盲区重现），
#   要么门禁永久失败（机制不可用）。两者都违背"判定层要有能力"的目标。
#
# 登记格式：<相对路径> | <书面说明原因>
#   · 以 # 开头的行为注释与说明，解析时跳过
#   · 路径支持前缀匹配（登记目录可覆盖其下全部文件）
#   · 门禁与检测器输出**一律以"未豁免项"为准**，豁免数量单独打印，绝不隐藏
#
# ⚠️ 纪律：豁免是"我已知情并书面确认"，不是"我看不见"。
#          凡登记于此的项，必须在下方写清为何保留，禁止空白或"暂不处理"这类无信息理由。
# ==============================================================================

# ── 图形资产的内嵌版本标注（2026-09-23 登记）──────────────────────────────────
# 背景：新增 assets/ 扫描后（此前是结构性盲区），检出这两张图内嵌 v1.6.0，
#       而当前总版本为 v3.0.0。
# 保留原因：两处 v1.6.0 的**语义不同且均非"图版本落后"**，机械改成 v3.0.0 属于伪造对齐：
#   · codex_vs_dsh_architecture_comparison.svg 标注为「v1.6.0 标准版」——
#     该数字描述的是图中对比所依据的**外部规范版本**（Codex 侧标准），
#     并非本工程的实施版本，改成本工程总版本会直接篡改图片要表达的信息；
#   · knowledge_base_layered_architecture.svg 标注为「知识法典 v1.6.0」——
#     同样描述**图中所述知识库内容的版本**，而非该图自身版本。
# 待办：若确认这两张图需要与当前实施版本对齐，正确做法是**重绘**并同时更新
#       内容与版本标注（属图形资产迭代，不在本轮"高危 + 检测器能力"授权范围内）。
assets/generated_images/codex_vs_dsh_architecture_comparison.svg | 图中「v1.6.0 标准版」描述的是对比所依据的外部规范版本，非本工程实施版本；机械改为 v3.0.0 会篡改图文含义。正确处置为重绘时同步更新，已在 REQ-047 验收清单登记待办。
assets/generated_images/knowledge_base_layered_architecture.svg | 图中「知识法典 v1.6.0」描述的是图中所载知识库内容的版本，非该图自身版本；机械对齐会篡改图文含义。正确处置为重绘时同步更新，已在 REQ-047 验收清单登记待办。

# ── 图形资产内的历史版本标注（2026-09-23 登记，随 v3.0.0 → v3.1.0 升级时新增）──
# 背景：实施总版本升到 v3.1.0 后，本图仍标注 v3.0.0，被 L3 报为"版本落后"。
# 保留原因：该图内的 v3.0.0 出现在「第五部分：快速通道（v3.0.0 新增）」这类**叙述文字**里，
# 描述的是"这部分内容是在 v3.0.0 那次迭代中加入的"——属**历史事实**，
# 不是"该图当前版本"。把它改成 v3.1.0 会变成"快速通道是 v3.1.0 新增的"，直接篡改历史。
# 判定口径说明：本项与上面两项**不同**——上面两项是"图的版本标注语义存疑"，
# 本项是"图内叙述引用了历史版本号"，后者按版本治理惯例本就不应随总版本变动。
# 正确处置：若该图需要整体刷新到 v3.1.0，应重绘并同步更新其自述版本，而非替换叙述中的历史数字。
assets/generated_images/gcm_gate_control_infographic.svg | 图内「（v3.0.0 新增）」是叙述该部分内容于 v3.0.0 迭代加入的历史事实，非该图当前版本；改为 v3.1.0 会篡改历史语义。正确处置为重绘时同步更新自述版本。
